# Python 3.13-compatible pins. The original requirements.txt targets 3.10-3.12;
# torch 2.3.1 / numpy 1.26 / pandas 2.2.2 publish no cp313 wheels, so these are
# bumped to the earliest releases that do. Install torch from the CPU index to
# avoid pulling ~2.5GB of CUDA libraries this project never uses.

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0

# --- Database ---
sqlalchemy==2.0.36

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
python-multipart==0.0.20

# --- ML / NLP ---
scikit-learn==1.6.0
pandas==2.2.3
numpy==2.2.1

# --- Utilities ---
python-dotenv==1.0.1

# Required by pydantic EmailStr in app/schemas/user.py (implicit dep missing
# from the original requirements.txt — the API fails to import without it).
email-validator==2.2.0
